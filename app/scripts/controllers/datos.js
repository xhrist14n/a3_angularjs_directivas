'use strict';

/**
 * @ngdoc function
 * @name adminApp.controller:DatosCtrl
 * @description
 * # DatosCtrl
 * Controller of the adminApp
 */
angular.module('adminApp').controller(
    'DatosCtrl', 
    function ($scope) {
        var id="datos";
    	activeMenu(id);
    }
);