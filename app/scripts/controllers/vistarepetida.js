'use strict';

/**
 * @ngdoc function
 * @name adminApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the adminApp
 */
angular.module('adminApp').controller(
    'VistaRepetidaCtrl', 
    function ($scope) {
        var id="vistarepetida";
    	activeMenu(id);
    }
);